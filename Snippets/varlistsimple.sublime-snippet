<snippet>
	<content><![CDATA[
${1:+|&|-}<${2:varListAlias}> : List("${3:option1}", "${4:option2}") { 
	name = "${5:...}"
	request = "${6:...}"
	default = "${7:...}"
	atomic = ${8:true|false}
	help = "${9:...}"
}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>varSimpleList</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>